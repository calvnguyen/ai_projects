Run the `create-supabase-setup` skill.

Bootstrap and manage a local-first Supabase backend with Docker for modern web app MVPs (React/Next.js + TypeScript + Tailwind). Use when setting up Supabase, scaffolding auth/database/storage, writing migrations or RLS policies, generating TypeScript types, seeding a local database, or deploying to hosted Supabase. Optimized for AI-assisted coding with Claude Code on macOS.

$ARGUMENTS
